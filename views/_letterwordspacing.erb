<div class="container">
  
  <!-- TITLE -->
  <div class="col-md-12"> 
    <h2> #6: Letter / Word Spacing </h2>
  </div>

  <!-- Nav tabs -->
  <ul class="nav nav-tabs">
    <li><a href="#hack" data-toggle="tab">Hack</a></li>
    <li><a href="#code" data-toggle="tab">Code</a></li>
    <!-- <li><a href="#html" data-toggle="tab">HTML</a></li> -->
  </ul>

  <!-- Tab panes -->
  <div class="tab-content">
    <!-- RESULTS TAB -->
    <div class="tab-pane active" id="hack">
      
      <!-- HTML FOR ENTRY HERE -->
      <div class="style-container"> 
        <p class="smallest"> getting larger</p>
        <p class="medium"> getting larger</p>
        <p class="largest"> getting larger</p>
        <p class="shrinking"> getting smaller</p>

        <!-- The word-spacing CSS property changes the inter-word space.  All modern browsers provide support for this  property. -->

        <p class="shortest">never liked you much anyways</p>
        <p class="wider">never liked you much anyways</p>
        <p class="collapsing">oh, wait.  Changed my mind.</p>
      </div>
      <!-- CSS FOR ENTRY HERE -->
        <style>
         .smallest{
            letter-spacing: 0.1em /* or px or "normal" but not %*/
          }

          .medium{
            letter-spacing: 0.5em
          }

          .largest{
            letter-spacing: 1em
          }

          .shrinking{
            letter-spacing: -0.08em
          }


          .shortest{
            word-spacing: 2em
          }

          .wider{
            word-spacing: 4em
          }

          .collapsing{
            word-spacing: -1em
          }
                  </style>
    </div>
    
    <!-- CODE TAB -->
    <div class="tab-pane" id="code">
      <div class="col-md-6 code"> 
        <h4> CSS </h4>
        <pre class="prettyprint"> 
          .smallest{
            letter-spacing: 0.1em /* or px or "normal" but not %*/
          }

          .medium{
            letter-spacing: 0.5em
          }

          .largest{
            letter-spacing: 1em
          }

          .shrinking{
            letter-spacing: -0.08em
          }


          .shortest{
            word-spacing: 2em
          }

          .wider{
            word-spacing: 4em
          }

          .collapsing{
            word-spacing: -1em
          }
        </pre>
      </div>
      <div class="col-md-6" id="code"> 
        <h4> HTML </h4>
        <pre class="prettyprint"> 
          &lt;!--  The letter-spacing CSS property changes the spacing between text characters on top of any spacing adjustments made by the typeface for kerning.  All browsers provide HTML support for this property. --&gt;

          &lt;p class="smallest"&gt; getting larger&lt;/p&gt;
          &lt;p class="medium"&gt; getting larger&lt;/p&gt;
          &lt;p class="largest"&gt; getting larger&lt;/p&gt;
          &lt;p class="shrinking"&gt; getting smaller&lt;/p&gt;

          &lt;!-- The word-spacing CSS property changes the inter-word space.  All modern browsers provide support for this  property. --&gt;

          &lt;p class="shortest"&gt;never liked you much anyways&lt;/p&gt;
          &lt;p class="wider"&gt;never liked you much anyways&lt;/p&gt;
          &lt;p class="collapsing"&gt;oh, wait.  Changed my mind.&lt;/p&gt;
        </pre>
      </div>
    </div>

  </div>
</div>

<!-- JS FOR TABS -->
<script>
  $('#myTab a').click(function (e) {
    e.preventDefault()
    $(this).tab('show')
  })
  $('#myTab a[href="#hack"]').tab('show') // Select tab by name
  $('#myTab a:first').tab('show') // Select first tab
  $('#myTab a:last').tab('show') // Select last tab
  $('#myTab li:eq(2) a').tab('show') // Select third tab (0-indexed)
</script>
